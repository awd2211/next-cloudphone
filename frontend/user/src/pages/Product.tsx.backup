import React from 'react';
import {
  Card,
  Row,
  Col,
  Typography,
  Button,
  Space,
  Divider,
  Tag,
  Timeline,
  Statistic,
} from 'antd';
import {
  MobileOutlined,
  CloudServerOutlined,
  ThunderboltOutlined,
  SafetyOutlined,
  GlobalOutlined,
  ApiOutlined,
  RocketOutlined,
  TeamOutlined,
  LineChartOutlined,
  SettingOutlined,
  CheckCircleOutlined,
  StarFilled,
} from '@ant-design/icons';
import { useNavigate } from 'react-router-dom';

const { Title, Paragraph, Text } = Typography;

/**
 * 产品介绍页
 *
 * 功能：
 * 1. 展示云手机平台核心功能和优势
 * 2. 介绍应用场景
 * 3. 技术架构说明
 * 4. 引导用户注册和购买
 */
const Product: React.FC = () => {
  const navigate = useNavigate();

  // 核心功能特性
  const coreFeatures = [
    {
      icon: <MobileOutlined style={{ fontSize: 48, color: '#1890ff' }} />,
      title: '真实Android环境',
      description:
        '基于Redroid容器技术，提供完整的Android系统环境，100%兼容原生应用，支持Android 7-13多版本。',
      tags: ['原生系统', '完全兼容', '多版本'],
    },
    {
      icon: <CloudServerOutlined style={{ fontSize: 48, color: '#52c41a' }} />,
      title: '弹性云端部署',
      description:
        '秒级创建和销毁设备，按需使用按需付费，无需购买昂贵的物理设备，支持批量管理数百台设备。',
      tags: ['按需使用', '秒级创建', '批量管理'],
    },
    {
      icon: <ThunderboltOutlined style={{ fontSize: 48, color: '#faad14' }} />,
      title: '极致性能体验',
      description:
        '基于高性能服务器，GPU加速渲染，WebRTC低延迟投屏，网络延迟<150ms，帧率稳定60fps。',
      tags: ['GPU加速', '低延迟', '高帧率'],
    },
    {
      icon: <SafetyOutlined style={{ fontSize: 48, color: '#f5222d' }} />,
      title: '企业级安全',
      description:
        '数据加密传输，设备隔离部署，完善的权限管理，支持VPN专线接入，符合等保三级标准。',
      tags: ['数据加密', '设备隔离', '等保认证'],
    },
    {
      icon: <GlobalOutlined style={{ fontSize: 48, color: '#722ed1' }} />,
      title: '全球节点覆盖',
      description:
        '在全球20+个地区部署节点，智能路由就近接入，支持海外应用测试和跨国业务部署。',
      tags: ['全球部署', '智能路由', '海外支持'],
    },
    {
      icon: <ApiOutlined style={{ fontSize: 48, color: '#13c2c2' }} />,
      title: '开放API接口',
      description:
        '提供完善的RESTful API和SDK，支持自动化脚本，轻松集成到现有系统，支持Webhook回调。',
      tags: ['RESTful API', 'SDK支持', 'Webhook'],
    },
  ];

  // 应用场景
  const useCases = [
    {
      icon: <RocketOutlined style={{ fontSize: 32, color: '#1890ff' }} />,
      title: 'App自动化测试',
      description:
        '在云端批量创建测试设备，执行UI自动化测试、兼容性测试、性能测试，大幅提升测试效率。',
      benefits: ['节省80%测试成本', '支持并发测试', '自动化脚本'],
    },
    {
      icon: <TeamOutlined style={{ fontSize: 32, color: '#52c41a' }} />,
      title: '游戏多开挂机',
      description:
        '同时运行多个游戏账号，支持24小时自动挂机，无需占用本地电脑资源，适合工作室批量运营。',
      benefits: ['支持100+设备', '24/7运行', '脚本自动化'],
    },
    {
      icon: <LineChartOutlined style={{ fontSize: 32, color: '#faad14' }} />,
      title: '社交营销推广',
      description:
        '批量管理社交账号，自动点赞、评论、私信，支持定时任务，提升营销效率和触达率。',
      benefits: ['批量账号管理', '智能防封', '定时任务'],
    },
    {
      icon: <SettingOutlined style={{ fontSize: 32, color: '#722ed1' }} />,
      title: '数据采集爬虫',
      description:
        '模拟真实手机环境，突破App反爬限制，获取App内数据，支持IP代理和设备指纹伪造。',
      benefits: ['突破反爬', 'IP池支持', '设备指纹'],
    },
  ];

  // 技术优势
  const techAdvantages = [
    {
      title: '容器化架构',
      description:
        '基于Docker和Redroid技术，每个云手机独立运行在容器中，资源隔离，快速部署，弹性伸缩。',
    },
    {
      title: '微服务架构',
      description:
        '采用NestJS微服务架构，服务解耦，高可用设计，支持水平扩展，99.9%服务可用性。',
    },
    {
      title: '实时通信',
      description:
        '基于WebRTC和WebSocket技术，实现低延迟屏幕投射和实时控制，延迟<150ms。',
    },
    {
      title: '智能调度',
      description:
        '自研资源调度算法，智能分配CPU、内存、GPU资源，保证每台设备的性能稳定性。',
    },
  ];

  // 产品数据
  const productStats = [
    { title: '服务客户', value: 5000, suffix: '+', prefix: '' },
    { title: '在线设备', value: 50000, suffix: '+', prefix: '' },
    { title: '全球节点', value: 20, suffix: '+', prefix: '' },
    { title: '服务可用性', value: 99.9, suffix: '%', prefix: '' },
  ];

  return (
    <div style={{ background: '#f0f2f5', minHeight: '100vh' }}>
      {/* Hero Section */}
      <div
        style={{
          background: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
          padding: '80px 24px',
          textAlign: 'center',
          color: 'white',
        }}
      >
        <Space direction="vertical" size="large" style={{ width: '100%' }}>
          <Title level={1} style={{ color: 'white', fontSize: 48, marginBottom: 0 }}>
            云手机平台 - 开启移动云端新时代
          </Title>
          <Paragraph
            style={{
              fontSize: 20,
              color: 'rgba(255, 255, 255, 0.9)',
              maxWidth: 800,
              margin: '0 auto',
            }}
          >
            基于容器技术的云端Android设备服务，为开发者、测试团队、游戏工作室提供高性能、低成本、易扩展的移动设备解决方案
          </Paragraph>
          <Space size="large" style={{ marginTop: 24 }}>
            <Button
              type="primary"
              size="large"
              icon={<RocketOutlined />}
              onClick={() => navigate('/plans')}
              style={{
                height: 50,
                fontSize: 18,
                background: 'white',
                color: '#667eea',
                borderColor: 'white',
              }}
            >
              立即开始免费试用
            </Button>
            <Button
              size="large"
              onClick={() => navigate('/pricing')}
              style={{
                height: 50,
                fontSize: 18,
                background: 'transparent',
                color: 'white',
                borderColor: 'white',
              }}
            >
              查看套餐定价
            </Button>
          </Space>
          <div style={{ marginTop: 24 }}>
            <Space size="middle" split={<Divider type="vertical" style={{ borderColor: 'rgba(255,255,255,0.3)' }} />}>
              <Text style={{ color: 'rgba(255, 255, 255, 0.8)' }}>
                ✓ 7天免费试用
              </Text>
              <Text style={{ color: 'rgba(255, 255, 255, 0.8)' }}>
                ✓ 无需信用卡
              </Text>
              <Text style={{ color: 'rgba(255, 255, 255, 0.8)' }}>
                ✓ 随时取消
              </Text>
            </Space>
          </div>
        </Space>
      </div>

      {/* 产品数据统计 */}
      <div style={{ maxWidth: 1200, margin: '-40px auto 0', padding: '0 24px' }}>
        <Card
          style={{
            boxShadow: '0 4px 20px rgba(0,0,0,0.1)',
            borderRadius: 12,
          }}
        >
          <Row gutter={[32, 32]}>
            {productStats.map((stat, index) => (
              <Col xs={24} sm={12} md={6} key={index}>
                <Statistic
                  title={<Text style={{ fontSize: 16 }}>{stat.title}</Text>}
                  value={stat.value}
                  suffix={stat.suffix}
                  prefix={stat.prefix}
                  valueStyle={{ color: '#1890ff', fontSize: 36, fontWeight: 'bold' }}
                />
              </Col>
            ))}
          </Row>
        </Card>
      </div>

      {/* 核心功能 */}
      <div style={{ maxWidth: 1200, margin: '80px auto', padding: '0 24px' }}>
        <div style={{ textAlign: 'center', marginBottom: 48 }}>
          <Tag color="blue" style={{ marginBottom: 16 }}>
            核心功能
          </Tag>
          <Title level={2}>为什么选择我们的云手机平台？</Title>
          <Paragraph style={{ fontSize: 16, color: '#666', maxWidth: 600, margin: '0 auto' }}>
            我们提供业界领先的云手机服务，助力您的业务快速发展
          </Paragraph>
        </div>

        <Row gutter={[24, 24]}>
          {coreFeatures.map((feature, index) => (
            <Col xs={24} sm={12} md={8} key={index}>
              <Card
                hoverable
                style={{
                  height: '100%',
                  borderRadius: 12,
                  transition: 'all 0.3s',
                }}
                bodyStyle={{ textAlign: 'center', padding: 32 }}
              >
                <div style={{ marginBottom: 16 }}>{feature.icon}</div>
                <Title level={4}>{feature.title}</Title>
                <Paragraph style={{ color: '#666', marginBottom: 16 }}>
                  {feature.description}
                </Paragraph>
                <Space size="small" wrap>
                  {feature.tags.map((tag, i) => (
                    <Tag key={i} color="blue">
                      {tag}
                    </Tag>
                  ))}
                </Space>
              </Card>
            </Col>
          ))}
        </Row>
      </div>

      {/* 应用场景 */}
      <div
        style={{
          background: 'white',
          padding: '80px 24px',
        }}
      >
        <div style={{ maxWidth: 1200, margin: '0 auto' }}>
          <div style={{ textAlign: 'center', marginBottom: 48 }}>
            <Tag color="green" style={{ marginBottom: 16 }}>
              应用场景
            </Tag>
            <Title level={2}>适用于多种业务场景</Title>
            <Paragraph style={{ fontSize: 16, color: '#666', maxWidth: 600, margin: '0 auto' }}>
              无论您是开发者、测试工程师、还是运营团队，都能从云手机平台中获益
            </Paragraph>
          </div>

          <Row gutter={[24, 24]}>
            {useCases.map((useCase, index) => (
              <Col xs={24} md={12} key={index}>
                <Card
                  hoverable
                  style={{
                    height: '100%',
                    borderRadius: 12,
                    border: '2px solid #f0f0f0',
                  }}
                >
                  <Space direction="vertical" size="middle" style={{ width: '100%' }}>
                    <Space>
                      {useCase.icon}
                      <Title level={4} style={{ marginBottom: 0 }}>
                        {useCase.title}
                      </Title>
                    </Space>
                    <Paragraph style={{ color: '#666', marginBottom: 0 }}>
                      {useCase.description}
                    </Paragraph>
                    <Space direction="vertical" size="small">
                      {useCase.benefits.map((benefit, i) => (
                        <Space key={i} size="small">
                          <CheckCircleOutlined style={{ color: '#52c41a' }} />
                          <Text>{benefit}</Text>
                        </Space>
                      ))}
                    </Space>
                  </Space>
                </Card>
              </Col>
            ))}
          </Row>
        </div>
      </div>

      {/* 技术优势 */}
      <div style={{ maxWidth: 1200, margin: '80px auto', padding: '0 24px' }}>
        <div style={{ textAlign: 'center', marginBottom: 48 }}>
          <Tag color="purple" style={{ marginBottom: 16 }}>
            技术优势
          </Tag>
          <Title level={2}>领先的技术架构</Title>
          <Paragraph style={{ fontSize: 16, color: '#666', maxWidth: 600, margin: '0 auto' }}>
            我们采用业界最先进的技术栈，确保服务的稳定性和性能
          </Paragraph>
        </div>

        <Timeline
          items={techAdvantages.map((advantage, index) => ({
            dot:
              index === 0 ? (
                <StarFilled style={{ fontSize: 16, color: '#faad14' }} />
              ) : undefined,
            children: (
              <Card style={{ marginBottom: 16 }}>
                <Title level={4}>{advantage.title}</Title>
                <Paragraph style={{ color: '#666', marginBottom: 0 }}>
                  {advantage.description}
                </Paragraph>
              </Card>
            ),
          }))}
        />
      </div>

      {/* CTA区域 */}
      <div
        style={{
          background: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
          padding: '80px 24px',
          textAlign: 'center',
        }}
      >
        <Space direction="vertical" size="large" style={{ width: '100%' }}>
          <Title level={2} style={{ color: 'white' }}>
            准备好开始了吗？
          </Title>
          <Paragraph
            style={{
              fontSize: 18,
              color: 'rgba(255, 255, 255, 0.9)',
              maxWidth: 600,
              margin: '0 auto',
            }}
          >
            加入5000+企业和开发者，体验业界领先的云手机服务
          </Paragraph>
          <Space size="large" style={{ marginTop: 24 }}>
            <Button
              type="primary"
              size="large"
              icon={<RocketOutlined />}
              onClick={() => navigate('/plans')}
              style={{
                height: 50,
                fontSize: 18,
                background: 'white',
                color: '#667eea',
                borderColor: 'white',
              }}
            >
              开始免费试用
            </Button>
            <Button
              size="large"
              onClick={() => navigate('/help')}
              style={{
                height: 50,
                fontSize: 18,
                background: 'transparent',
                color: 'white',
                borderColor: 'white',
              }}
            >
              联系我们
            </Button>
          </Space>
        </Space>
      </div>
    </div>
  );
};

export default Product;
