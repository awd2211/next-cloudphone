/**
 * Utility hooks for React Query + Zod validation
 */

export {
  useValidatedQuery,
  useValidatedMutation,
  ensureArray,
  safeGet,
} from './useValidatedQuery';

export type { UseValidatedQueryOptions } from './useValidatedQuery';
