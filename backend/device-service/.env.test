# Test Environment Configuration
NODE_ENV=test

# Database Configuration (Test Database)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=cloudphone_device_test

# Redis Configuration (Use test Redis instance or mock)
REDIS_HOST=localhost
REDIS_PORT=6379

# RabbitMQ Configuration (Mock in tests)
RABBITMQ_URL=amqp://admin:admin123@localhost:5672/cloudphone_test

# JWT Configuration
JWT_SECRET=test-jwt-secret-for-e2e-testing-only-not-secure

# Docker Configuration
DOCKER_HOST=unix:///var/run/docker.sock

# Port Range for ADB
ADB_PORT_START=15000
ADB_PORT_END=16000

# Service Ports
PORT=30002
CONSUL_HOST=localhost
CONSUL_PORT=8500

# Quota Service URL (Mocked in tests)
QUOTA_SERVICE_URL=http://localhost:30001

# Feature Flags for Lifecycle Automation (Disable in tests)
ENABLE_AUTO_CLEANUP=false
ENABLE_AUTO_SCALING=false
ENABLE_FAULT_DETECTION=false
ENABLE_STATE_RECOVERY=false
ENABLE_AUTO_BACKUP=false
