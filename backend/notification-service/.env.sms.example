# ========================================
# SMS 短信服务配置
# ========================================

# ========== SMS 通用配置 ==========
SMS_PRIMARY_PROVIDER=twilio              # 主要提供商: twilio | aws-sns | messagebird
SMS_FALLBACK_PROVIDERS=aws-sns,messagebird  # 备用提供商（逗号分隔）

# ========== Twilio 配置 ==========
# https://www.twilio.com/console
TWILIO_ENABLED=true
TWILIO_ACCOUNT_SID=your_account_sid      # 从 Twilio Console 获取
TWILIO_AUTH_TOKEN=your_auth_token        # 从 Twilio Console 获取
TWILIO_FROM_NUMBER=+1234567890           # 发送方号码（需要在 Twilio 购买）
TWILIO_TIMEOUT=30000                     # 超时时间（毫秒）
TWILIO_RETRIES=3                         # 重试次数

# ========== AWS SNS 配置 ==========
# https://console.aws.amazon.com/sns/
AWS_SNS_ENABLED=true
AWS_ACCESS_KEY_ID=your_access_key        # AWS IAM 用户访问密钥
AWS_SECRET_ACCESS_KEY=your_secret_key    # AWS IAM 用户密钥
AWS_REGION=us-east-1                     # AWS 区域
AWS_SNS_SENDER_ID=CloudPhone             # 发送方显示名称（某些国家支持）
AWS_SNS_TIMEOUT=30000
AWS_SNS_RETRIES=3

# ========== MessageBird 配置 ==========
# https://dashboard.messagebird.com/
MESSAGEBIRD_ENABLED=true
MESSAGEBIRD_API_KEY=your_api_key         # 从 MessageBird Dashboard 获取
MESSAGEBIRD_ORIGINATOR=CloudPhone        # 发送方名称（11个字符以内）
MESSAGEBIRD_TIMEOUT=30000
MESSAGEBIRD_RETRIES=3

# ========================================
# 短信服务商选择指南
# ========================================

# Twilio (推荐全球使用)
# - 优点: 覆盖200+国家，送达率高，文档完善，功能最全
# - 缺点: 价格较高
# - 价格: $0.0075 - $0.10 每条（取决于国家）
# - 注册: https://www.twilio.com/try-twilio

# AWS SNS (推荐 AWS 生态用户)
# - 优点: 与 AWS 服务集成，价格便宜，全球覆盖
# - 缺点: 功能相对简单，需要 AWS 账号
# - 价格: $0.00645 - $0.13 每条（取决于国家）
# - 注册: https://aws.amazon.com/

# MessageBird (推荐欧洲/亚洲用户)
# - 优点: 欧洲亚洲覆盖好，价格适中，API 简单
# - 缺点: 美洲覆盖稍弱
# - 价格: €0.01 - €0.10 每条（取决于国家）
# - 注册: https://www.messagebird.com/signup

# ========================================
# 价格对比示例（美国）
# ========================================
# Twilio:      $0.0075/条
# AWS SNS:     $0.00645/条
# MessageBird: €0.008/条 (~$0.009)

# ========================================
# 快速开始
# ========================================

# 1. 选择一个提供商并注册账号
# 2. 获取 API 密钥和其他凭证
# 3. 复制此文件为 .env 并填写相应的配置
# 4. 设置 SMS_PRIMARY_PROVIDER 为你选择的提供商
# 5. 启动服务并测试

# 测试命令:
# curl -X POST http://localhost:30006/sms/send-otp \
#   -H "Content-Type: application/json" \
#   -d '{"phoneNumber": "+1234567890", "code": "123456"}'
