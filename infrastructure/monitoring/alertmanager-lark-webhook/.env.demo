# Lark (飞书) Webhook 演示配置
# 这是一个配置模板，用于演示如何部署 Lark Webhook 服务
#
# 重要提示：
# 1. 将此文件复制为 .env: cp .env.demo .env
# 2. 填入您的真实飞书 Webhook URL
# 3. 不要将包含真实凭据的 .env 文件提交到版本控制
#
# 获取飞书 Webhook URL 的步骤：
# 1. 登录飞书管理后台 (https://open.feishu.cn/)
# 2. 进入要接收通知的群组
# 3. 群组设置 → 机器人 → 添加机器人 → 自定义机器人
# 4. 设置机器人名称和描述
# 5. 安全设置（可选）:
#    - 关键词: 设置必须包含的关键词（如"告警"）
#    - 签名验证: 启用后会生成签名密钥
# 6. 复制生成的 Webhook URL

# 飞书 Webhook URL（必填）
# 格式: https://open.feishu.cn/open-apis/bot/v2/hook/YOUR_WEBHOOK_TOKEN
LARK_WEBHOOK_URL=YOUR_LARK_WEBHOOK_URL_HERE

# 飞书机器人签名密钥（可选）
# 如果在飞书机器人设置中启用了签名验证，需要配置此项
# 格式: 一个随机字符串，由飞书生成
LARK_SECRET=

# 服务监听端口（可选，默认 5001）
PORT=5001

# 日志级别（可选，默认 info）
# 可选值: debug, info, warn, error
LOG_LEVEL=info

# ==========================================
# 示例配置（替换为您的真实值）
# ==========================================
# LARK_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# LARK_SECRET=abcdef1234567890
# PORT=5001
# LOG_LEVEL=info

# ==========================================
# 安全建议
# ==========================================
# 1. 不要在公开仓库中提交真实的 Webhook URL
# 2. 定期轮换 Webhook URL（在飞书管理后台重新生成）
# 3. 启用签名验证增强安全性
# 4. 限制 IP 访问（如果飞书支持）
# 5. 监控 Webhook 调用频率，防止滥用
